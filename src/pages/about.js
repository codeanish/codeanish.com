import React from 'react';
import Layout from '../components/layout'

const About = () => {
    return(
        <Layout>
            <h1>About Me</h1>
        </Layout>
    );
};

export default About;